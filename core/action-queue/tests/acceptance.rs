#![allow(clippy::ignored_unit_patterns)]

mod acceptance {
    mod auto_queue;
    mod cancellation;
    mod common;
    mod contexts;
    mod executors;
    mod observer;
    mod replace_or_queue;
    mod revert;
    mod state;
    mod version;
}

[package]
  name = "proton-mail-ids"
  version = "0.1.0"
  edition.workspace = true

[dependencies]
  proton-mail-api = { workspace = true }
  proton-core-common = { workspace = true }
  serde = { workspace = true }
  stash = { workspace = true }

[lints]
  workspace = true

[features]
  default = ["sql"]
  sql = ["proton-mail-api/sql"]

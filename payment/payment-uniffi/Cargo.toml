[package]
name = "proton-payment-uniffi"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
# Empty crate not needed for now
# proton-payment-sdk = { workspace = true }

[lints]
workspace = true

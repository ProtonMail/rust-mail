[project]
name = "changelog"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.8",
    "gitpython>=3.1.44",
    "jinja2>=3.1.6",
]

[project.scripts]
changelog = "changelog:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

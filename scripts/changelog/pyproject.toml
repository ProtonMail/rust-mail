[project]
name = "changelog"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["click>=8", "gitpython>=3", "jinja2>=3"]

[project.scripts]
changelog = "changelog:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

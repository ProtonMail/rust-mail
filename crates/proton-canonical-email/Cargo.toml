[package]
name = "proton-canonical-email"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = {workspace = true}
stash = {workspace = true, optional = true}

[lints]
workspace = true

[features]
default = []
sql = ["dep:stash"]
